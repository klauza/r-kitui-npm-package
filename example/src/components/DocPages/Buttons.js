import React from 'react'
import styled from 'styled-components';

import DocReusable from '../DocReusable';
const Buttons = () => {

  return (
    <div>
      <DocReusable active="buttons">Buttons Docs</DocReusable>
    </div>
  )
}

export default Buttons
